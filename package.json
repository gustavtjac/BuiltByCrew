{
  "name": "builtbycrew",
  "version": "1.0.0",
  "description": "Autonomous webapp factory — orchestrated by Claude Code",
  "scripts": {
    "deploy:landing": "ts-node scripts/deploy-landing.ts",
    "deploy:github": "ts-node scripts/create-github-repo.ts",
    "post:social": "ts-node scripts/post-social.ts",
    "post:scheduled": "ts-node scripts/post-social.ts"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
